@page "/Delete"
@page "/Delete/{IdTecnico:int}"
@inject TecnicosServices tecnicosServices
@inject NavigationManager navigationManager
@rendermode InteractiveServer
<PageTitle>Eliminar Prioridad</PageTitle>

@if (Tecnicos != null)
{
    <div class="container">
        <div class="card shadow-header">
            <!-- Header -->
            <div class="card-header  bg-primary text-white text-center py-2 rounded">
                <h3><strong>Eliminar Prioridad</strong></h3>
            </div>
            <div class="card-header">
                <h3 class="text-danger">¿Estás seguro de que quieres eliminar la prioridad?</h3>
            </div>
            <!-- Body -->
            <div class="container">
                <div class="row">
                    <div class="col-md-6">
                        <div class="card shadow-lg">
                            <div class="card-header">
                                <h3 class="text-center"><strong>Datos de la prioridad</strong></h3>
                            </div>
                            <div class="card-body">
                                <div class="border-primary border-2 borde">
                                    <div class="card-body">
                                        <div class="row">
                                            <div class="col-md-8">
                                                <ul class="list-unstyled">
                                                    <li><strong>PrioridadId:</strong> @Tecnicos.IdTecnico</li>
                                                    <li><strong>Descripción:</strong> @Tecnicos.Nombre</li>
                                                    <li><strong>Días Compromiso:</strong> @Tecnicos.SueldoHora</li>
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- Footer -->
            <div class="card-footer">
                <button type="button" class="btn btn-outline-danger" @onclick="EliminarPrioridad"><i class="bi bi-trash-fill"></i> Eliminar</button>
                <button type="button" class="btn btn-outline-primary" @onclick="Volver"><i class="bi bi-box-arrow-left"></i> Volver</button>
            </div>
        </div>
    </div>
}
else
{
    <p>Prioridad no encontrada.</p>
}

@code {
    [Parameter]
    public int IdTecnico { get; set; }
    public Tecnicos? Tecnicos { get; set; } = new Tecnicos();

    protected override async Task OnInitializedAsync()
    {
        if (IdTecnico != 0)
        {
            Tecnicos = await tecnicosServices.Buscar(IdTecnico);
        }
    }

    public async Task EliminarPrioridad()
    {
        if (IdTecnico == 0 || Tecnicos == null)
        {
            return;
        }

        if (await tecnicosServices.Eliminar(IdTecnico))
        {
            Volver();
        }
    }

    public void Volver()
    {
        navigationManager.NavigateTo("/Index");
    }
}
